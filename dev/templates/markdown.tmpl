## Netperf Test Results

**Test Configuration:**
- Test: ${TEST_NAME}
- Protocol: ${PROTOCOL}  
- Direction: ${DIRECTION}
- Date: ${TIMESTAMP}
- Duration: ${ELAPSED_TIME}s

**Performance Metrics:**
| Metric | Value |
|--------|-------|
| Throughput | ${THROUGHPUT} ${THROUGHPUT_UNITS} |
| Transaction Rate | ${TRANSACTION_RATE} tps |
| Mean Latency | ${MEAN_LATENCY} μs |
| Min Latency | ${MIN_LATENCY} μs |
| Max Latency | ${MAX_LATENCY} μs |
| P90 Latency | ${P90_LATENCY} μs |
| P99 Latency | ${P99_LATENCY} μs |

**System Information:**
- Local Host: ${LOCAL_HOST}
- Remote Host: ${REMOTE_HOST}
- Local CPU: ${LOCAL_CPU_UTIL}%
- Remote CPU: ${REMOTE_CPU_UTIL}%

**Test Command:**
```
${COMMAND_LINE}
```

---
*Generated by netperf ${NETPERF_VERSION}*
