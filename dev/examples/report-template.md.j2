# Netperf Test Report

**Generated**: {{ timestamp|format_timestamp }}
**Test Suite**: Performance Baseline
**Environment**: Production Network

## Executive Summary

This report presents the results of {{ results|length }} netperf test(s) conducted to measure network performance across different traffic patterns.

### Key Metrics

- **Total Tests Executed**: {{ results|length }}
- **Average Throughput**: {{ results|avg_value('throughput')|format_throughput }}
- **Maximum Throughput**: {{ results|max_value('throughput').throughput|format_throughput }}
- **Test Duration**: {{ duration|format_duration }}

## Detailed Results

{% for result in results|sort_by('throughput', reverse=True) %}
### {{ result.test_name }}

- **Direction**: {{ result.direction }}
{% if result.throughput > 0 -%}
- **Throughput**: **{{ result.throughput|format_throughput }}**
{% endif -%}
{% if result.latency > 0 -%}
- **Latency**: **{{ result.latency|format_latency }}**
{% endif -%}
- **CPU Utilization**:
  - Local: {{ result.cpu_local|format_percent }}
  - Remote: {{ result.cpu_remote|format_percent }}

{% endfor %}

## Analysis

{% if results|avg_value('throughput') > 9000 -%}
✅ Network throughput is excellent (>9 Gbps average)
{% elif results|avg_value('throughput') > 5000 -%}
⚠️  Network throughput is moderate (5-9 Gbps)
{% else -%}
❌ Network throughput is below expectations (<5 Gbps)
{% endif %}

## Recommendations

1. Review tests with throughput < {{ results|avg_value('throughput')|format_throughput }}
2. Monitor CPU utilization trends over time
3. Consider running stress tests for capacity planning

---

*Report generated by netperf-template {{ timestamp|format_timestamp }}*
